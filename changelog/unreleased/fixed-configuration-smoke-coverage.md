### Fixed

- Validate the documented minimal and complete bundle configuration in fresh Symfony 8 hosts against both `v1.0.0` and the current development version, including effective configuration, container parameters, rendered HTML and CSV output.
