- Add a generic HTTP/API data provider foundation with explicit capabilities,
  page/offset/cursor pagination, replaceable transport and mapping contracts,
  an optional Symfony HttpClient adapter, normalized remote failures and
  guarded streaming exports.
