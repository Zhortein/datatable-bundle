{% if actions is not empty %}
    <div class="d-grid gap-1 zhortein-datatable__row-actions-list">
        {% for action in actions %}
            {{ include(theme.template('_list_action.html.twig'), {theme: theme, action: action}, with_context=false) }}
        {% endfor %}
    </div>
{% endif %}
