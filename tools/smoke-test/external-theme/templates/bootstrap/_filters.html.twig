{% for filter in definition.filters %}
    {% include theme.template('_filter.html.twig') with {
        filter: filter,
        htmlId: htmlId,
        value: filters[filter.name]|default(null),
        translationDomain: definition.translationDomain
    } %}
{% endfor %}
